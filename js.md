# JavaScript - მოკლე ცნობარი

## ობიექტები (Objects)

```javascript
user["job"] = "Developer"; // ფრჩხილების სინტაქსი (კარგია, როცა გასაღები ცვლადშია)
delete user.age; // ობიექტში თვისების ამოღება (წაშლა) delete ოპერატორით
```

## Number, Math, NaN და Infinity

### რიცხვების მეთოდები

```javascript
.toFixed(2);         // აბრუნებს ტექსტს მითითებული ათწილადის სიზუსტით: "5.68"
Number.parseInt("10px");    // ტექსტიდან იღებს მთელ რიცხვს: 10
Number.parseFloat("3.14");  // ტექსტიდან იღებს ათწილადს: 3.14
Number.isNaN(value);        // ამოწმებს, არის თუ არა მნიშვნელობა NaN
```

### Math ობიექტის მეთოდები

- `Math.round(4.6)` ➞ `5` (ამრგვალებს უახლოეს მთელამდე).
- `Math.floor(4.9)` ➞ `4` (ყოველთვის ამრგვალებს ქვემოთ).
- `Math.ceil(4.1)` ➞ `5` (ყოველთვის ამრგვალებს ზემოთ).
- `Math.max(10, 20, 5)` ➞ `20` (პოულობს მაქსიმალურს).
- `Math.min(10, 20, 5)` ➞ `5` (პოულობს მინიმალურს).
- `Math.random()` ➞ აბრუნებს შემთხვევით რიცხვს `0`-დან `1`-მდე.
- `Math.pow()`    ➞ ხარისხში აყვანა
- `Math.sqrt()`   ➞ ფესვის ამოღება რიცხვიდან


```javascript
text.trim(); // სივრცეების წაშლა (თავში და ბოლოში)
text.includes("Java"); // true (ამოწმებს შეიცავს თუ არა)
text.indexOf("J"); // 7 (აბრუნებს პირველივე ნაპოვნი სიმბოლოს ინდექსს)
text.replace("JavaScript", "World"); // ჩანაცვლება
text.charAt(2) //აბრუნებს გადაცემულ ინდექსზე არსებულ სიმბოლოს
text.substr(5,24) //გადავცემთ ორ ინდექს საიდან დაიწყოს მოჭრა და რამდენი სიმბოლო მოჭრას

let words = "apple,banana,kiwi".split(","); // მასივად გადაქცევა ["apple", "banana", "kiwi"]
```
## Array (მასივის) მეთოდები

```javascript
slice(1, 6); // ნაწილის ამოჭრა (იწყება 1-დან, მე-6 არ შედის) ქმნის ახალ მასივს
splice(1,4"change") // წაშლა/დამატება 1-ლი არგუმენტი: საიდან დავიწყოთ (საწყისი ინდექსი).
 // მე-2 არგუმენტი: რამდენი ელემენტი წავშალოთ.მე-3 (და შემდგომი) არგუმენტი: რა ელემენტები 
 // ჩავსვათ წაშლილების ადგილას.(ცვლის იგივე მასივს
push(150) // ამატებს ერთ ან რამდენიმე ახალ ელემენტს მასივის ბოლოში და ცვლის ორიგინალ მასივს.
join("/") // აერთიანებს მასივის ყველა ელემენტს და აქცევს მათ ერთიან ტექსტად (სტრინგად). შეგიძლია 
 //მიუთითო გამყოფი სიმბოლო. ორიგინალ მასივს არ ცვლის.
pop() //შლის მასივის ბოლო ელემენტს და უკან აბრუნებს ამ წაშლილ ელემენტს. ცვლის ორიგინალ მასივს.
unshift() // 1ს ან რამდენიმე ახალ ელემენტს მასივის დასაწყისში (პირველ პოზიციაზე) და ცვლის ორიგინალ მასივს.
shift() // შლის მასივის პირველ ელემენტს და უკან აბრუნებს ამ წაშლილ ელემენტს. ცვლის ორიგინალ მასივს
reverse() // აბრუნებს მასივის ელემენტების თანმიმდევრობას უკუღმა ცვლის ორიგინალ მასივს.
sort() // ახარისხებს მასივის ელემენტებს და ცვლის ორიგინალ მასივს. (default) ის ყველაფერს ახარისხებს როგორც 
// ტექსტს (ანბანის მიხედვით), ამიტომ რიცხვებზე პირდაპირ გამოყენებამ შეიძლება არასწორი შედეგი მოგცეს 
```